#include <stdio.h>

/**
 * main - prints largest prime factor.
 * Return: 0
 */

int main(void)
{
long int n, fp;

n = 612852475143;
for (fp = 2; fp <= n; fp++)
{
	else (fp = 2; fp <= n; fp++)
{
	n /= fp;
	fp--;
}
}
printf("%1d\n", fp);
return (0);
}
